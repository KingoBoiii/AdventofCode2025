<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<OutputType>Exe</OutputType>
		<TargetFramework>net10.0</TargetFramework>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
	</PropertyGroup>

	<Target Name="CopyFromSolutionDir" AfterTargets="Build">
		<Copy SourceFiles="$(SolutionDir)assets\Input_Day01.txt"
			  DestinationFolder="$(OutputPath)\assets"
			  SkipUnchangedFiles="true" />

		<Copy SourceFiles="$(SolutionDir)assets\Input_Day02.txt"
			  DestinationFolder="$(OutputPath)\assets"
			  SkipUnchangedFiles="true" />

		<Copy SourceFiles="$(SolutionDir)assets\Input_Day03.txt"
			  DestinationFolder="$(OutputPath)\assets"
			  SkipUnchangedFiles="true" />

		<Copy SourceFiles="$(SolutionDir)assets\Input_Day04.txt"
			  DestinationFolder="$(OutputPath)\assets"
			  SkipUnchangedFiles="true" />

		<Copy SourceFiles="$(SolutionDir)assets\Input_Day05.txt"
			  DestinationFolder="$(OutputPath)\assets"
			  SkipUnchangedFiles="true" />

		<Copy SourceFiles="$(SolutionDir)assets\Input_Day5.txt"
			  DestinationFolder="$(OutputPath)\assets"
			  SkipUnchangedFiles="true" />
	</Target>

</Project>
